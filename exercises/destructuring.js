let student = {
	name: "Alan",
	notes: [10, 16, 17],
	average: null,
};

// TODO ...

[firstname, notes, average] = [student.name, student.notes, student.average];

// constantes
console.log(firstname, notes, average);